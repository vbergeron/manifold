import Config

# Nothing to override: the defaults in `config/config.exs` *are* the development setup, and
# `config/runtime.exs` layers the `MANIFOLD_*` environment on top. This file exists because
# `import_config "#{config_env()}.exs"` requires one per environment.
